## 📐 Diagrama de Entidad – `servidores` (Mermaid)

```mermaid
erDiagram
    SERVIDORES {
        UUID id PK "Identificador único"
        TEXT empresa "Nombre de la empresa"
        TEXT nombre_servidor "Nombre del servidor"
        TEXT vpn_nombre "Perfil VPN (Radmin)"
        TEXT vpn_contraseña "Contraseña VPN"
        TEXT vpn_ip "IP asignada en VPN"
        JSONB usuarios "Lista de usuarios con roles"
        TEXT tailscale_tnet "Nombre del tailnet"
        JSONB tailscale_config "Configuración Tailscale"
        TEXT email_despliegue "Correo de despliegue"
        TEXT password_despliegue "Contraseña del correo"
        TIMESTAMP created_at "Fecha de creación"
    }
